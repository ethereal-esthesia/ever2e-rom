# ROMS Build Outputs

`ROMS/` is treated as a generated build-output directory.

- ROM binaries and generated checksum files in `ROMS/` are not tracked.
- Regenerate local artifacts as needed from scripts/Make targets.
- Keep durable design/spec docs under `specs/`, not under `ROMS/`.
