MEMORY {
    ROM: start = $C000, size = $4000, file = %O, fill = yes, fillval = $00;
}

SEGMENTS {
    CODE:    load = ROM, start = $F000, type = ro;
    VECTORS: load = ROM, start = $FFFA, type = ro;
}
